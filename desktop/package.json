{
  "name": "@cloudcli-ai/desktop",
  "version": "1.0.0",
  "description": "Secure remote desktop client for CloudCLI",
  "author": "CloudCLI UI Contributors",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "commonjs",
  "main": "out/main/index.js",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "electron-vite dev",
    "build": "npm run typecheck && electron-vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify:fuses": "node scripts/verify-fuses.cjs",
    "package:mac": "npm run build && electron-builder --config electron-builder.config.ts --mac --universal --publish never",
    "package:win": "npm run build && electron-builder --config electron-builder.config.ts --win --x64 --publish never"
  },
  "dependencies": {
    "electron-updater": "6.8.9"
  },
  "devDependencies": {
    "@electron/fuses": "2.1.3",
    "@types/node": "22.19.7",
    "electron": "43.4.0",
    "electron-builder": "26.15.3",
    "electron-vite": "5.0.0",
    "typescript": "5.9.3",
    "vite": "7.3.6",
    "vitest": "4.1.10"
  }
}
