# AI 使用看板需求对应说明

## 1.「AI 使用」页签

| 你提出的要求 | 页面对应位置、显示内容 | 当前情况 |
| --- | --- | --- |
| “AI 使用与共创”改名为“AI 使用” | 第一个页签叫「AI 使用」，页面标题为「AI 使用统计」 | 已对应 |
| 个人会话次数 | 顶部「会话次数」卡片＋下方「用户使用明细」中的会话次数列 | 已对应；当前租户合计 **601** |
| 个人 AI 活跃时长 | 「AI 活跃时长」卡片＋用户明细中的时长列 | 已对应；当前合计 **6h 5m** |
| 只需要发布 Skill 数量、Skill 被调用次数 | 两张对应指标卡片＋用户明细中的两列 | 已对应；当前为 **0、600** |
| 不能只有一个 Skill 调用总数，要看每个 Skill | 页面下方「各 Skill 被调用明细」 | 已对应；逐项显示名称、发布者、发布时间、调用次数、调用人数、最近调用时间 |
| 同名 Skill 也要能够区分 | Skill 名称下显示 ID，发布者单列展示 | 已对应；两个“SQL 分析”分别统计，各 **21 次** |
| DAU／MAU 放进新看板 | 「日活用户 DAU」「近 30 日活跃用户 MAU」卡片；趋势图可切换指标 | 已对应；当前 **4、41** |
| 调用 Skill 种类数暂时不做 | 无此指标 | 按要求未加入 |
| 不需要 AI 贡献度指数 | 无综合评分、贡献度指数 | 按要求未加入 |

这里的“发布 Skill 数量为 0”和下方有 **30 个 Skill** 不矛盾：卡片统计所选日期内的新发布数量；模拟 Skill 发布时间为 **8 月 1 日**，早于当前筛选起点 **8 月 14 日**。下方仍会展示这些历史发布 Skill 的调用情况。

## 2. Hook 与 Agent 模板

| 你提出的要求 | 页面对应位置、显示内容 | 当前情况 |
| --- | --- | --- |
| Hook 执行统计要有 | 独立「Hook 执行统计」页签 | 已对应；显示执行次数、成功／失败／未结束次数、平均耗时，并可打开执行记录 |
| 记录性 Hook 要单独展示 | 独立「记录性 Hook」页签 | 已对应，与执行统计分开 |
| 区分不同的会话记录 Hook | Hook 名称下显示 ID，并列出后置行为、记录类型、版本 | 已对应；`hook-session-a`、`hook-session-b` 不会因同名合并 |
| 统计后置行为产生的具体记录信息 | 「具体业务记录 · 字段统计」＋每行「记录明细」 | 已对应；不只是统计 Hook 执行了多少次 |
| 展示具体记录字段 | 点击「记录明细」 | 已对应；包含记录时间、用户、工作区、会话，以及归档条数、质量分、处理结果等已开放字段 |
| number 字段可以统计 | 字段统计中的「统计方式」「统计结果」「有效样本数」 | **数值聚合已有，但字段类型复用尚未完成** |
| 加入 Agent 模板使用记录 | 「Agent 模板使用」页签 | 已对应；展示应用次数、应用人数、会话次数、活跃使用人数、时长；提供「应用记录」「关联会话」 |

记录性 Hook 目前能直接看到的例子：

| 具体记录字段 | 当前统计示例 |
| --- | --- |
| 归档条数 | 200 条有效记录，字段求和 **600 条** |
| 归档质量分 | 200 个有效值，平均 **89 分** |
| 复核耗时 | 字段求和 **1,000 分钟** |
| SQL 行数 | 字段求和 **6,800 行** |

这些值按 **Hook、后置行为、版本、类型、单位** 分别统计，不把不同业务字段混加。

## 3. 权限与页面操作

| 你提出的要求 | 当前页面／实现对应 | 当前情况 |
| --- | --- | --- |
| 增加租户粒度管理员，可以查看报表 | 「租户全员／仅本人」范围选择；后端校验 `tenant_admin` | 已实现；普通用户只能查看本人，原 admin 权限保留 |
| 看板能筛选 | 开始日期、结束日期、用户 ID、工作区 ID、搜索名称；各模块有补充筛选 | 已对应 |
| 看板能排序 | 点击表头切换升降序 | 已对应；针对全部匹配结果，不限当前页 |
| 看板能聚合 | 点击「分组汇总」 | 已对应；按用户、工作区、日／周／月，以及 Hook／模板等维度汇总 |
| 聚合结果能进一步筛选 | 「分组汇总」中的指标、最小值、最大值 | 已对应 |
| 刷新结果要二次确认 | 右上角「刷新结果」→确认弹窗 | 已对应；仅重新查询已发布报表，**不会提前启动统计** |
| 导出统一异步，具体内容再议 | 右上角「异步导出」→导出任务弹窗 | **待定**；异步基础能力已有，字段、格式未配置，目前不能下载 |
| 用户 ID、工作区 ID 不要上下增减框 | 筛选栏两个普通文本输入框 | 已对应；无增减箭头，保留正整数校验 |
| 不需要模型提供方筛选 | 主筛选栏已移除 | 已对应；但「分组汇总」仍保留按提供方分组，模板关联会话内也还有提供方筛选 |

最上方橙色栏里的“预览租户／预览身份”只是模拟测试控件，**不是正式页面允许用户自行切换权限的功能**。

## 4. 统计口径、性能与运行方式

这些要求不能只凭页面外观判断，我同时核对了实现。

| 你确认的要求 | 当前实现／页面说明 |
| --- | --- |
| 使用时间按交互区间累加，例子是 2＋3＝5 分钟 | 每轮「用户请求开始→AI 响应完成」累加；轮间等待不计，不设置单轮时长上限 |
| 跨日拆分统计 | 23:58→次日 00:03，分别记前一天 **2 分钟**、后一天 **3 分钟**；结束时间迟到时补算相关历史日期 |
| 只统计原生 Skill 工具和 `/skill` | 已按此收集；斜杠命令须能验证对应真实 Skill，匹配的斜杠与工具调用去重 |
| 不统计 Hook、Agent Graph、Top Skill 的 Skill 调用 | 按调用来源排除；Read／Bash 读取 Skill 文件也不计入 |
| 非实时，闲时统计，env 配置凌晨 2 点 | 已有 `AI_USAGE_RUN_AT=02:00` 等配置；页面显示数据截止时间、上次成功时间、下次开始时间、时区 |
| DAU／MAU 同样夜间统计 | 与其他报表使用同一夜间发布批次；MAU 是近 30 日去重人数，不是每天 DAU 相加 |
| 不要因打开报表大量扫描消息 | 页面查询已生成的统计结果；消息解析与增量汇总放在夜间后台执行，未引入 MQ |
| 不少于 1,000 条模拟数据并测试 | 当前 **3,556 条**，插入隔离内存数据库并经过实际统计流程；未污染业务数据库 |
| 不要停止预览服务 | 已由 macOS 后台托管，配置退出后重启；当前服务仍在运行 |
